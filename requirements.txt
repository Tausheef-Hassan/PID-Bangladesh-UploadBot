# ── Bot pipeline ──────────────────────────────────────────────────────────────
opencv-python-headless
numpy
requests
Pillow
beautifulsoup4
google-cloud-translate
google-api-python-client
google-auth
google-auth-httplib2
google-genai
pywikibot
urllib3

# ── Control panel (web: process type) ─────────────────────────────────────────
flask
gunicorn
toolforge-weld     # Wikimedia's own Toolforge API client: certs, retries, errors
humanize
croniter
# OAuth 2.0 is plain HTTP against meta's rest.php endpoints, so requests is
# the whole client — mwoauth and requests-oauthlib were only needed for 1.0a.
